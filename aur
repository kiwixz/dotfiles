#!/bin/sh -e

if [[ $# -eq 0 ]]; then
    echo "Usage: $0 package" >2
    exit 1
fi

workdir="$HOME/.aur/$1-$RANDOM"
mkdir -p "$workdir"
cd "$workdir"

curl -LO "https://aur.archlinux.org/cgit/aur.git/snapshot/$1.tar.gz"
tar -xf "$1.tar.gz"
cd "$1"
makepkg -sirc

rm -rf "$workdir"
